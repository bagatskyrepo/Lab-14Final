# 1. Використовуємо SQLite в пам'яті (::memory:)
# Це означає, що база створюється з нуля при запуску тесту і зникає після нього.
# Вона НІЯК не перетинається з твоєю основною базою.
spring.datasource.url=jdbc:sqlite::memory:
spring.datasource.driver-class-name=org.sqlite.JDBC

# 2. Змушуємо Hibernate створити таблиці з нуля
spring.jpa.hibernate.ddl-auto=create-drop

# 3. Показуємо SQL запити (щоб бачити в консолі, що відбувається)
spring.jpa.show-sql=true

# 4. Діалект (має бути такий самий, як у тебе в основному конфігу, 
# але зазвичай цей підходить для більшості бібліотек SQLite)
spring.jpa.database-platform=org.hibernate.community.dialect.SQLiteDialect
# АБО (якщо старий): spring.jpa.database-platform=org.hibernate.dialect.SQLiteDialect
# ... SQLite settings ...

# JWT Settings for Testing
jwt.secret=SecretKeyForTestingMustBeLongEnough1234567890
jwt.expiration=3600000

# 👇 ADD THIS LINE:
jwt.refresh-expiration=86400000